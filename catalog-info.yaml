apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name }}
  description: An example component created from a template.
  annotations:
    github.com/project-slug: LuizRojas/${{ values.name }}
spec:
  type: service
  owner: ${{ values.owner }}
  lifecycle: experimental